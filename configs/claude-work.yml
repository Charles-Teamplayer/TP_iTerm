session: claude-work
windows:
  - name: teamplean-website
    root: /Users/teample.casper/claude/teamplean-website
    commands:
      - sleep 0 && bash ~/.claude/scripts/tab-status.sh starting 'teamplean-website' && unset CLAUDECODE && claude --dangerously-skip-permissions --continue
  - name: universalMAC_Converter_gensys_calude
    root: /Users/teample.casper/claude/universalMAC_Converter_gensys_calude
    commands:
      - sleep 5 && bash ~/.claude/scripts/tab-status.sh starting 'universalMAC_Converter_gensys_calude' && unset CLAUDECODE && claude --dangerously-skip-permissions --continue
  - name: ims_Auto_Contact APP
    root: "~/claude/ims_Auto_Contact APP"
    commands:
      - sleep 10 && bash ~/.claude/scripts/tab-status.sh starting 'ims_Auto_Contact APP' && unset CLAUDECODE && claude --dangerously-skip-permissions --continue
  - name: AppleTV_ScreenSaver.app
    root: /Users/teample.casper/claude/AppleTV_ScreenSaver.app
    commands:
      - sleep 15 && bash ~/.claude/scripts/tab-status.sh starting 'AppleTV_ScreenSaver.app' && unset CLAUDECODE && claude --dangerously-skip-permissions --continue
  - name: TP-BP_Knowledgebase
    root: /Users/teample.casper/claude/TP-BP_Knowledgebase
    commands:
      - sleep 20 && bash ~/.claude/scripts/tab-status.sh starting 'TP-BP_Knowledgebase' && unset CLAUDECODE && claude --dangerously-skip-permissions --continue
  - name: TP_Status
    root: /Users/teample.casper/claude/TP_Status
    commands:
      - sleep 25 && bash ~/.claude/scripts/tab-status.sh starting 'TP_Status' && unset CLAUDECODE && claude --dangerously-skip-permissions --continue
  - name: TP_iTerm
    root: ~/claude/TP_iTerm
    commands:
      - sleep 30 && bash ~/.claude/scripts/tab-status.sh starting 'TP_iTerm' && unset CLAUDECODE && claude --dangerously-skip-permissions --continue
  - name: SJ_MindMap
    root: ~/claude/SJ_MindMap
    commands:
      - sleep 35 && bash ~/.claude/scripts/tab-status.sh starting 'SJ_MindMap' && unset CLAUDECODE && claude --dangerously-skip-permissions --continue
  - name: testCalculator
    root: /Users/teample.casper/claude/testCalculator
    commands:
      - sleep 40 && bash ~/.claude/scripts/tab-status.sh starting 'testCalculator' && unset CLAUDECODE && claude --dangerously-skip-permissions --continue

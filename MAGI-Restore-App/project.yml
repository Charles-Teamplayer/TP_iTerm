name: MAGI-Restore
options:
  bundleIdPrefix: com.teample
  deploymentTarget:
    macOS: "13.0"
  xcodeVersion: "15.0"
settings:
  base:
    SWIFT_VERSION: "5.9"
    CODE_SIGN_IDENTITY: "-"
    CODE_SIGNING_REQUIRED: "NO"
    INFOPLIST_FILE: "Sources/Info.plist"
    PRODUCT_BUNDLE_IDENTIFIER: "com.teample.MAGI-Restore"
    ARCHS: "arm64"
    ONLY_ACTIVE_ARCH: "NO"
targets:
  MAGI-Restore:
    type: application
    platform: macOS
    sources:
      - path: Sources
